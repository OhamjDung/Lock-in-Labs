google-genai
python-dotenv
pydantic
firebase-admin
fastapi
uvicorn[standard]
websockets
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0
scikit-learn>=1.0.0
ffpyplayer>=4.5.3
Pillow>=9.0.0
# hitherdither removed — using native dithering implementation

# Semantic Memory / RAG dependencies
chromadb>=0.4.0
sentence-transformers>=2.2.0  # Optional: Use OpenAI embeddings instead (set OPENAI_API_KEY)
# openai>=1.0.0  # Uncomment if using OpenAI embeddings instead of local models